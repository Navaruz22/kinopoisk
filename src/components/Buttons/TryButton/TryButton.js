import React from "react";
import styles from "./TryButton.module.css";

const TryButton = () => {
  return <button className={styles.try_button}>Попробовать Плюс</button>;
};

export default TryButton;
